services:
  worker:
    image: python:3.11-alpine
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - API_URL=${API_URL:-http://localhost:28000}
      - API_TOKEN=${API_TOKEN:-token}
    command: sh -c "pip install requests && python worker.py"
